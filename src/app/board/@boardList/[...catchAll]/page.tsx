import React from 'react'

//default page는 nav로 이동시 새로고침해야 보여지는데
// catchAll page는 nav로 이동시 새로고침 없이 보여짐
// => default와 달리 새로고침해도 계속 똑같은 페이지 보여짐
export default function BoardListCatchAllPage() {
  return (
    <div>💫BoardListCatchAllPage</div>
  )
}
